#pragma once

/*#include "Usuario.h"

class PersonalDeVigilancia : public Usuario
{
public:
	PersonalDeVigilancia::PersonalDeVigilancia(int id, std::string nombres, std::string apellidos, std::string email, std::string usuario, std::string password, std::string permiso) :
		Usuario(id, nombres, apellidos, email, usuario, password, permiso) {

	}
    PersonalDeVigilancia::PersonalDeVigilancia(Usuario* u) {
        id = u->getId();
        nombres = u->getNombres();
        apellidos = u->getApellidos();
        email = u->getEmail();
        usuario = u->getUsuario();
        password = u->getPassword();
        permiso = u->getPermiso();
    }
};
*/
